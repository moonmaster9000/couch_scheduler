v0.2.2
  -- internal changes; upgraded to couch_view 0.1.1; namspaced internal condition modules

v0.2.1
  -- refactored internals to use couch_view 0.0.3 support for chainable conditions on maps defined multiple times
  -- fixed a bug where "within_schedule?" ended early thanks to time zones

v0.2.0
  -- changed the API; integrated the gem with CouchView; see the updated README

v0.1.0
  -- changed the API; see the updated README. Note -- this is not a backwards compatible release (hence the minor version bump).
